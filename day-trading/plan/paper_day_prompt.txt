You are launched HEADLESS by Windows Task Scheduler (\Stocks\C37MorningLaunch) to run today's ENTIRE C37 paper-trading session end-to-end. You have FULL AUTHORITY and NOBODY IS READING YOUR REPLIES: never ask a question, never end your turn early, never stop because something looks ambiguous or because OTHER files in the repo are uncommitted (a parallel campaign session owns those; ignore them completely). Your turn ends ONLY when the session is closed after 15:00 ET with the EOD ledger committed and pushed.

HEADLESS DEATH RULE (Day-14 postmortem, 2026-08-24): in headless -p mode the turn IS the session. Ending the turn starts a 600s background-task wait, then the process is terminated. Never yield expecting a Monitor or cron to re-invoke you. Pace the whole day inside ONE turn with foreground until-loop waits of AT MOST 8 minutes per tool call (longer waits get backgrounded at the 600s cap).

FIRST ACTION, before anything else: write the day-file skeleton data/paper_days/{today}.json (mode, config C37, empty trades list) and commit it -- this is the liveness signal the watchdog and backup launcher key on.

CAPABILITY PROBE, immediately after the skeleton and before any protocol work (Day-16 postmortem, 2026-08-26 -- LIVENESS IS NOT CAPABILITY): run three cheap probes and write their results into the day JSON at ops.capability_probe -- (1) python -c "print(1)", (2) git status --short, (3) one MCP call, get_equity_quotes SPY. Day 16 wrote its skeleton, passed the launcher's 720s liveness check as "confirmed live", and could not run python, could not commit, and had ZERO market data, because --permission-mode acceptEdits auto-approves EDITS ONLY; an interactive session had to take over at 07:17. Note the trap: the day file is BOTH the liveness signal AND the launcher's no-double-launch lock, so a crippled session blocks its own replacement until a human moves the file aside -- if you are blocked, say that explicitly in the runbook. If any probe is DENIED: say so loudly in the day JSON errors[] and in a data/paper_days/PERMISSION_BLOCKED_{today}.flag runbook, append a line to data/scheduler_log.txt, send a PushNotification, and DO NOT improvise a substitute -- never hand-rank, never swap in a non-Robinhood feed for live decisions (feed-calibration fix #8), never credit an entry for a gap window (OUTAGE rule 3). A blocked day is an OPS failure, not a flat day: pnl 0, counts_as_traded_day false, denominator unchanged. Keep the session alive and re-probe until 15:00 -- the tradeable window is 09:30-14:30, so a mid-morning unblock still trades.

Then: read C:\cornell\stocks-automation\.claude\skills\daytrading-morning.md and follow the TOP section exactly (HISTORY below never overrides). NO REAL ORDERS EVER; ONE position at a time; flat $15k tickets ($10k last) to $100k/day; entries until 14:30; all exits by 15:00 with the 14:57 ladder. Use the rank command (never hand-rank); coordinator-owned Trigger C 1-minute polling ([TAKEABLE NOW] only, never [STALE], no delegated pattern detection); crossed-set latch; fill-arming rule at every arming; SPREAD/DEPTH/CHASE vetoes as three separate rates; clock rule (TZ env broken: NEVER compute ET by hand -- every wait is `python plan/wait_until.py HH:MM`, which uses zoneinfo and is DST-safe; Monitors on UTC); read `python plan/market_calendar.py` SESSION line at start and use ITS entry_cutoff / exit_end / ladder times (half-days close 13:00: last ticket 12:00, ladder 12:50-12:59); watcher = `python plan/paper_watch.py --book` (see WATCHER below); get_equity_historicals batched <=10 with returned-set assert. HALAL: only verdict PASS is armable; unverified = FAIL. `rank` membership is a PREFILTER only -- at every arming run `python plan/live_halal.py SYM --json` (fresh halal_check with the rulings overlay) and arm only on verdict PASS; record the result in the day JSON under halal_detail[sym]. BENCHMARK: score against -$163/traded day (C37F: C37's own rules on the full-coverage honest pool, -$72,673/445d, 18-23 negative months) and against break-even. The old $1,517/day figure is RETIRED -- it was a bar-coverage artifact. A no-trade day is a NO-LOSS day under this ruleset, not a missed opportunity: report it as such rather than as underperformance. If monitoring dies mid-position, follow OUTAGE / DEAD-MONITOR SETTLEMENT. TAKE NOTES AND COMMIT AND PUSH THE LEDGER AS THE DAY RUNS (stage only your own files -- git add specific paths, never -A). Write data/paper_days/{today}.json and .md, update NOTES-DAYTRADING.md, EOD summary, final commit and push.

USEFUL TOOLING (established Day 15, 2026-08-25): plan/scan_sweep.py parses a spilled/saved run_scan dump into the compact candidate report with day-long NEW/GONE state (state file data/paper_days/scan_state_{date}.json -- seed its drop-lists from the prior day's verdicts). plan/append_bars.py appends 1-min bars from CLI args to data/rh_bars CSVs (header begins_at,...) with a post-arm stop check (--after=UTC_ISO). (csv_to_watchjson.py / BARS_JSON are the LEGACY single-symbol watcher path -- superseded by `paper_watch.py --book`, which reads data/rh_bars CSVs directly; see WATCHER.) RH premarket-dark names read CALM-GAP FAIL from a missing 7AM bar: complete gap7 from shared.massive.minute_bars (Polygon) and LOG the completion loudly.

HEARTBEAT (2026-09-01, required -- the launcher and the 12:00/14:45 watchdog now key on it): the moment the capability probe is GREEN, and then on EVERY cycle of the day (at least every 5 minutes), touch data/paper_days/SESSION_ALIVE_{today}.flag (write the current ET time into it). No heartbeat = the watchdog raises NO/STALE HEARTBEAT and a human is paged. A day file alone is no longer proof of life.

WATCHER (2026-09-01 rewrite, plan/paper_watch.py): ONE process for the whole book. On each FILL run `python plan/paper_watch.py --open SYM --entry PX --shares N --ticket K --prev-close PC --entry-bar-utc <ISO>` (creates the date-stamped state file; pre-entry bars are never replayed), then keep `python plan/paper_watch.py --book` running as ONE background task (its EXIT_MODE default is c37 = today's rules: -8% hard stop, 20/10/40 pressure trail, bank 1/3 at +25%; do NOT pass --exit-mode unless the skill's TOP section says the mode changed). Feed it by appending completed 1-min bars to data/rh_bars/{SYM}_{today}.csv every minute (plan/append_bars.py) and, from 14:45, write data/paper/quotes_{today}.json {SYM:{bid,ask,ts}} every minute from get_equity_quotes so the ladder prices off a real bid. The watcher OWNS the end-of-day ladder (14:50/14:55/14:58/14:59 or the half-day equivalents), books every exit, writes data/paper_days/{today}.flatten.json (with exit_parity vs the 14:59 close) and {today}.equity.json, logs CB-WOULD-FIRE circuit-breaker levels (measurement only, never acts), unlinks its own state files, and writes the WATCH_ALIVE_{today}.json heartbeat. Read its output tail for EXIT-* events and copy them into the ledger; never flatten by hand while --book is running. `python plan/posn.py` reports the open book. If the watcher dies mid-position, restart it (`--book` reloads today's state files) and log the gap; if it cannot be restarted, follow OUTAGE / DEAD-MONITOR SETTLEMENT. Any state file dated another day makes it refuse to start (exit 2): move that file aside and log why.
